import React from 'react';
import FontAwesome from 'react-fontawesome';

function Icon(props) {
    return (
        <FontAwesome
            name='chevron-right'
        />
    );
}

export default Icon;
